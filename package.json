{
  "name": "cambios-devoluciones-extension-ui",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "shopify": "shopify",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy --config=custom",
    "dev": "shopify app dev --config=custom",
    "build": "react-router build"
  },
  "dependencies": {},
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "jsrnieto",
  "private": true
}
